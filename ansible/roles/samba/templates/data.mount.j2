# /etc/systemd/system/data.mount

[Unit]
Description=Data Mount

[Mount]
What=UUID={{ blkid_md0p1.stdout }}
Where=/data
Type=xfs
Options=defaults
#DirectoryMode=0755

[Install]
WantedBy=multi-user.target
